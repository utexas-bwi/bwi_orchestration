default_flips: 
   - gateway: robot1       # flip to a specific rule to a gateway
     rule:  
       name: /robot1/application/cmd_vel
       node: None
       type: subscriber
   - gateway: robot1       # flip to a specific rule to a gateway
     rule:  
       name: /robot1/application/odom
       node: None
       type: publisher
   - gateway: robot1       # flip to a specific rule to a gateway
     rule:  
       name: /robot1/application/scan_filtered
       node: None
       type: publisher
   - gateway: robot1       # flip to a specific rule to a gateway
     rule:  
       name: /map
       node: None
       type: publisher
   - gateway: robot1       # flip to a specific rule to a gateway
     rule:  
       name: /static_map
       node: None
       type: service
   - gateway: robot1       # flip to a specific rule to a gateway
     rule:  
       name: /tf
       node: None
       type: publisher
   # - gateway: robot1       # flip to a specific rule to a gateway
   #   rule:  
   #     name: /tf_changes
   #     node: None
   #     type: publisher
   - gateway: robot2       # flip to a specific rule to a gateway
     rule:  
       name: /robot2/application/cmd_vel
       node: None
       type: subscriber
   - gateway: robot2       # flip to a specific rule to a gateway
     rule:  
       name: /robot2/application/odom
       node: None
       type: publisher
   - gateway: robot2       # flip to a specific rule to a gateway
     rule:  
       name: /robot2/application/scan_filtered
       node: None
       type: publisher
   - gateway: robot2       # flip to a specific rule to a gateway
     rule:  
       name: /map
       node: None
       type: publisher
   - gateway: robot2       # flip to a specific rule to a gateway
     rule:  
       name: /static_map
       node: None
       type: service
   - gateway: robot2       # flip to a specific rule to a gateway
     rule:  
       name: /tf
       node: None
       type: publisher
   # - gateway: robot2       # flip to a specific rule to a gateway
   #   rule:  
   #     name: /tf_changes
   #     node: None
   #     type: publisher
   - gateway: robot3       # flip to a specific rule to a gateway
     rule:  
       name: /robot3/application/cmd_vel
       node: None
       type: subscriber
   - gateway: robot3       # flip to a specific rule to a gateway
     rule:  
       name: /robot3/application/odom
       node: None
       type: publisher
   - gateway: robot3       # flip to a specific rule to a gateway
     rule:  
       name: /robot3/application/scan_filtered
       node: None
       type: publisher
   - gateway: robot3       # flip to a specific rule to a gateway
     rule:  
       name: /map
       node: None
       type: publisher
   - gateway: robot3       # flip to a specific rule to a gateway
     rule:  
       name: /static_map
       node: None
       type: service
   - gateway: robot3       # flip to a specific rule to a gateway
     rule:  
       name: /tf
       node: None
       type: publisher
   # - gateway: robot3       # flip to a specific rule to a gateway
   #   rule:  
   #     name: /tf_changes
   #     node: None
   #     type: publisher
   - gateway: robot4       # flip to a specific rule to a gateway
     rule:  
       name: /robot4/application/cmd_vel
       node: None
       type: subscriber
   - gateway: robot4       # flip to a specific rule to a gateway
     rule:  
       name: /robot4/application/odom
       node: None
       type: publisher
   - gateway: robot4       # flip to a specific rule to a gateway
     rule:  
       name: /robot4/application/scan_filtered
       node: None
       type: publisher
   - gateway: robot4       # flip to a specific rule to a gateway
     rule:  
       name: /map
       node: None
       type: publisher
   - gateway: robot4       # flip to a specific rule to a gateway
     rule:  
       name: /static_map
       node: None
       type: service
   - gateway: robot4       # flip to a specific rule to a gateway
     rule:  
       name: /tf
       node: None
       type: publisher
   # - gateway: robot4       # flip to a specific rule to a gateway
   #   rule:  
   #     name: /tf_changes
   #     node: None
   #     type: publisher
